# Studiio Backend — Kopiere diese Datei nach .env und fülle die Werte aus.
# .env wird nicht versioniert.

PORT=8788
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: true = pro PDF-Seite KI-Zusammenfassung (teuer). Weglassen oder false = nur Textauszug.
# BUILD_MATERIAL_CONTEXT_PAGE_AI=true
