# Frontend (Vite) — in .env.local oder .env
VITE_SUPABASE_URL=your-supabase-url-here
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Backend-URL in Production (z. B. nach Deployment)
# VITE_API_BASE=https://your-api.vercel.app

# Backend: Siehe backend/.env.example und lege backend/.env an (PORT, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY).
